<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

	<%= link_to root_path, class: "navbar-brand", id: 'logo' do %>
		PhotoApp
	<% end %>

	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="nav navbar-nav ml-auto">
			<li class="nav-item dropdown">
				<%= link_to (current_user ? current_user.email : 'Guest'), '#', class: 'nav-link dropdown-toggle', role: 'button', id: 'navbarDropDown', 'data-toggle': 'dropdown' %>
				<div class="dropdown-menu dropdown-menu-right" aria-labelledby="usersDropdown">
					<% unless current_user %>
						<%= link_to 'Log In', new_user_session_path, class: 'dropdown-item' %>
						<%= link_to 'Sign Up', new_user_registration_path, class: 'dropdown-item' %>
					<% else %>
						<%#= link_to 'View Profile', current_user, class: 'dropdown-item' %>
						<%= link_to 'Edit Profile', edit_user_registration_path(current_user), class: 'dropdown-item' %>
						<div class="dropdown-divider"></div>
						<%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
					<% end %>
				</div>
			</li>
		</ul>
	</div>
</nav>
